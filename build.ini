[logging]
level = INFO
format = %%(asctime)s - %%(levelname)s: %%(message)s

[project]
name = MockatorProject
prefix = ch.hsr.ifs.mockator

[mappings]
root = root
features = features
bundles = bundles
releng = releng
tests = tests
examples = examples
helps = helps

[build]
target = Target
