//!CtorDependencyToClassWithNonVirtualDtorShouldBeIgnored
//@main.cpp
struct IDependency {
  ~IDependency();
};

struct SUT{
  SUT(IDependency& d) : d(d) {}
private:
  IDependency& d;
};

void testSUT() {
  SUT sut(dependency);
}