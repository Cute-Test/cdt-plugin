//!StrstrChecksIfNotNull1
//@main.cpp
#include <cstring>
#include "iostream.h"

int main(){
	char mail[] = "admin@web.com";
	if(strstr(mail, "@")) {
		std::cout << mail;
	}
	while(strstr(mail, "@")) {
		std::cout << mail;
	}
	for(; strstr(mail, "@");) {
		std::cout << mail;
	}
	char* ptr = strstr(mail, "@") ? strstr(mail, "@") : mail;
	char* x = strstr(mail, "@");
	x = strstr(mail, "@");
	bool y = strstr(mail, "@");
	y = strstr(mail, "@");
	bool z1 = strstr(mail, "@") || true;
	bool z2 = strstr(mail, "@") && true;
	do {
		std::cout << mail;
	} while(strstr(mail, "@"));
	
	int index = strstr(mail, "@") - mail;
}
//=
#include <cstring>
#include <string>
#include "iostream.h"

int main(){
	std::string mail = "admin@web.com";
	if (mail.find("@") != std::string::npos) {
		std::cout << mail;
	}
	while (mail.find("@") != std::string::npos) {
		std::cout << mail;
	}
	for (; mail.find("@") != std::string::npos;) {
		std::cout << mail;
	}
	char* ptr =
			mail.find("@") != std::string::npos ?
					strstr(mail.c_str(), "@") : mail.c_str();
	char* x = strstr(&*mail.begin(), "@");
	x = strstr(&*mail.begin(), "@");
	bool y = mail.find("@") != std::string::npos;
	y = mail.find("@") != std::string::npos;
	bool z1 = mail.find("@") != std::string::npos || true;
	bool z2 = mail.find("@") != std::string::npos && true;
	do {
		std::cout << mail;
	} while (mail.find("@") != std::string::npos);
	
	int index = strstr(mail.c_str(), "@") - mail.c_str();
}


//!StrstrChecksIfNotNull2
//@main.cpp
#include <cstring>
#include "iostream.h"

int main(){
	char mail[] = "admin@web.com";
	if(nullptr != strstr(mail, "@")) {
		std::cout << mail;
	}
	if(strstr(mail, "@") != nullptr) {
		std::cout << mail;
	}
	while(strstr(mail, "@") != nullptr) {
		std::cout << mail;
	}
	for(; strstr(mail, "@") != nullptr;) {
		std::cout << mail;
	}
	char* ptr = strstr(mail, "@") != nullptr ? strstr(mail, "@") : mail;
	bool y = strstr(mail, "@") != nullptr;
	y = strstr(mail, "@") != nullptr;
	bool z1 = strstr(mail, "@") != nullptr || true;
	bool z2 = strstr(mail, "@") != nullptr && true;
	do {
		std::cout << mail;
	} while(strstr(mail, "@") != nullptr);
}
//=
#include <cstring>
#include <string>
#include "iostream.h"

int main(){
	std::string mail = "admin@web.com";
	if (mail.find("@") != std::string::npos) {
		std::cout << mail;
	}
	if (mail.find("@") != std::string::npos) {
		std::cout << mail;
	}
	while (mail.find("@") != std::string::npos) {
		std::cout << mail;
	}
	for (; mail.find("@") != std::string::npos;) {
		std::cout << mail;
	}
	char* ptr =
			mail.find("@") != std::string::npos ?
					strstr(mail.c_str(), "@") : mail.c_str();
	bool y = mail.find("@") != std::string::npos;
	y = mail.find("@") != std::string::npos;
	bool z1 = mail.find("@") != std::string::npos || true;
	bool z2 = mail.find("@") != std::string::npos && true;
	do {
		std::cout << mail;
	} while (mail.find("@") != std::string::npos);
}


//!StrstrChecksIfNotNull3
//@main.cpp
#include <cstring>
#include "iostream.h"

int main(){
	char mail[] = "admin@web.com";
	if(0 != strstr(mail, "@")) {
		std::cout << mail;
	}
	if(strstr(mail, "@") != 0) {
		std::cout << mail;
	}
	while(strstr(mail, "@") != 0) {
		std::cout << mail;
	}
	for(; strstr(mail, "@") != 0;) {
		std::cout << mail;
	}
	char* ptr = strstr(mail, "@") != 0 ? strstr(mail, "@") : mail;
	bool y = strstr(mail, "@") != 0;
	y = strstr(mail, "@") != 0;
	bool z1 = strstr(mail, "@") != 0 || true;
	bool z2 = strstr(mail, "@") != 0 && true;
	do {
		std::cout << mail;
	} while(strstr(mail, "@") != 0);
}
//=
#include <cstring>
#include <string>
#include "iostream.h"

int main(){
	std::string mail = "admin@web.com";
	if (mail.find("@") != std::string::npos) {
		std::cout << mail;
	}
	if (mail.find("@") != std::string::npos) {
		std::cout << mail;
	}
	while (mail.find("@") != std::string::npos) {
		std::cout << mail;
	}
	for (; mail.find("@") != std::string::npos;) {
		std::cout << mail;
	}
	char* ptr =
			mail.find("@") != std::string::npos ?
					strstr(mail.c_str(), "@") : mail.c_str();
	bool y = mail.find("@") != std::string::npos;
	y = mail.find("@") != std::string::npos;
	bool z1 = mail.find("@") != std::string::npos || true;
	bool z2 = mail.find("@") != std::string::npos && true;
	do {
		std::cout << mail;
	} while (mail.find("@") != std::string::npos);
}


//!StrstrChecksIfNotNull4
//@main.cpp
#include <cstring>
#include "iostream.h"

int main(){
	char mail[] = "admin@web.com";
	if(NULL != strstr(mail, "@")) {
		std::cout << mail;
	}
	if(strstr(mail, "@") != NULL) {
		std::cout << mail;
	}
	while(strstr(mail, "@") != NULL) {
		std::cout << mail;
	}
	for(; strstr(mail, "@") != NULL;) {
		std::cout << mail;
	}
	do {
		std::cout << mail;
	} while(strstr(mail, "@") != NULL);
}
//=
#include <cstring>
#include <string>
#include "iostream.h"

int main(){
	std::string mail = "admin@web.com";
	if (mail.find("@") != std::string::npos) {
		std::cout << mail;
	}
	if (mail.find("@") != std::string::npos) {
		std::cout << mail;
	}
	while (mail.find("@") != std::string::npos) {
		std::cout << mail;
	}
	for (; mail.find("@") != std::string::npos;) {
		std::cout << mail;
	}
	do {
		std::cout << mail;
	} while (mail.find("@") != std::string::npos);
}


//!StrstrChecksIfNull1
//@main.cpp
#include <cstring>
#include "iostream.h"

int main(){
	char mail[] = "admin@web.com";
	if(!strstr(mail, "@")) {
		std::cout << mail;
	}
	while(!strstr(mail, "@")) {
		std::cout << mail;
	}
	for(; !strstr(mail, "@");) {
		std::cout << mail;
	}
	char* ptr = !strstr(mail, "@") ? strstr(mail, "@") : mail;
	bool y = !strstr(mail, "@");
	y = !strstr(mail, "@");
	bool z1 = !strstr(mail, "@") || true;
	bool z2 = !strstr(mail, "@") && true;
	do {
		std::cout << mail;
	} while(!strstr(mail, "@"));
}
//=
#include <cstring>
#include <string>
#include "iostream.h"

int main(){
	std::string mail = "admin@web.com";
	if (mail.find("@") == std::string::npos) {
		std::cout << mail;
	}
	while (mail.find("@") == std::string::npos) {
		std::cout << mail;
	}
	for (; mail.find("@") == std::string::npos;) {
		std::cout << mail;
	}
	char* ptr =
			mail.find("@") == std::string::npos ?
					strstr(mail.c_str(), "@") : mail.c_str();
	bool y = mail.find("@") == std::string::npos;
	y = mail.find("@") == std::string::npos;
	bool z1 = mail.find("@") == std::string::npos || true;
	bool z2 = mail.find("@") == std::string::npos && true;
	do {
		std::cout << mail;
	} while (mail.find("@") == std::string::npos);
}


//!StrstrChecksIfNull2
//@main.cpp
#include <cstring>
#include "iostream.h"

int main(){
	char mail[] = "admin@web.com";
	if(nullptr == strstr(mail, "@")) {
		std::cout << mail;
	}
	if(strstr(mail, "@") == nullptr) {
		std::cout << mail;
	}
	while(strstr(mail, "@") == nullptr) {
		std::cout << mail;
	}
	for(; strstr(mail, "@") == nullptr;) {
		std::cout << mail;
	}
	char* ptr = strstr(mail, "@") == nullptr ? strstr(mail, "@") : mail;
	bool y = strstr(mail, "@") == nullptr;
	y = strstr(mail, "@") == nullptr;
	bool z1 = strstr(mail, "@") == nullptr || true;
	bool z2 = strstr(mail, "@") == nullptr && true;
	do {
		std::cout << mail;
	} while(strstr(mail, "@") == nullptr);
}
//=
#include <cstring>
#include <string>
#include "iostream.h"

int main(){
	std::string mail = "admin@web.com";
	if (mail.find("@") == std::string::npos) {
		std::cout << mail;
	}
	if (mail.find("@") == std::string::npos) {
		std::cout << mail;
	}
	while (mail.find("@") == std::string::npos) {
		std::cout << mail;
	}
	for (; mail.find("@") == std::string::npos;) {
		std::cout << mail;
	}
	char* ptr =
			mail.find("@") == std::string::npos ?
					strstr(mail.c_str(), "@") : mail.c_str();
	bool y = mail.find("@") == std::string::npos;
	y = mail.find("@") == std::string::npos;
	bool z1 = mail.find("@") == std::string::npos || true;
	bool z2 = mail.find("@") == std::string::npos && true;
	do {
		std::cout << mail;
	} while (mail.find("@") == std::string::npos);
}


//!StrstrChecksIfNull3
//@main.cpp
#include <cstring>
#include "iostream.h"

int main(){
	char mail[] = "admin@web.com";
	if(0 == strstr(mail, "@")) {
		std::cout << mail;
	}
	if(strstr(mail, "@") == 0) {
		std::cout << mail;
	}
	while(strstr(mail, "@") == 0) {
		std::cout << mail;
	}
	for(; strstr(mail, "@") == 0;) {
		std::cout << mail;
	}
	char* ptr = strstr(mail, "@") == 0 ? strstr(mail, "@") : mail;
	bool y = strstr(mail, "@") == 0;
	y = strstr(mail, "@") == 0;
	bool z1 = strstr(mail, "@") == 0 || true;
	bool z2 = strstr(mail, "@") == 0 && true;
	do {
		std::cout << mail;
	} while(strstr(mail, "@") == 0);
}
//=
#include <cstring>
#include <string>
#include "iostream.h"

int main(){
	std::string mail = "admin@web.com";
	if (mail.find("@") == std::string::npos) {
		std::cout << mail;
	}
	if (mail.find("@") == std::string::npos) {
		std::cout << mail;
	}
	while (mail.find("@") == std::string::npos) {
		std::cout << mail;
	}
	for (; mail.find("@") == std::string::npos;) {
		std::cout << mail;
	}
	char* ptr =
			mail.find("@") == std::string::npos ?
					strstr(mail.c_str(), "@") : mail.c_str();
	bool y = mail.find("@") == std::string::npos;
	y = mail.find("@") == std::string::npos;
	bool z1 = mail.find("@") == std::string::npos || true;
	bool z2 = mail.find("@") == std::string::npos && true;
	do {
		std::cout << mail;
	} while (mail.find("@") == std::string::npos);
}


//!StrstrChecksIfNull4
//@main.cpp
#include <cstring>
#include "iostream.h"

int main(){
	char mail[] = "admin@web.com";
	if(NULL == strstr(mail, "@")) {
		std::cout << mail;
	}
	if(strstr(mail, "@") == NULL) {
		std::cout << mail;
	}
	while(strstr(mail, "@") == NULL) {
		std::cout << mail;
	}
	for(; strstr(mail, "@") == NULL;) {
		std::cout << mail;
	}
	do {
		std::cout << mail;
	} while(strstr(mail, "@") == NULL);
}
//=
#include <cstring>
#include <string>
#include "iostream.h"

int main(){
	std::string mail = "admin@web.com";
	if (mail.find("@") == std::string::npos) {
		std::cout << mail;
	}
	if (mail.find("@") == std::string::npos) {
		std::cout << mail;
	}
	while (mail.find("@") == std::string::npos) {
		std::cout << mail;
	}
	for (; mail.find("@") == std::string::npos;) {
		std::cout << mail;
	}
	do {
		std::cout << mail;
	} while (mail.find("@") == std::string::npos);
}


//!StrstrIfStatementWithCstr
//@main.cpp
#include <cstring>
#include <string>
#include "iostream.h"

int main(){
	char mail[] = "admin-web.com";
	std::string at = "@";
	if (nullptr == strstr(mail, at.c_str())) {
		std::cout << "invalid";
	}
}
//=
#include <cstring>
#include <string>
#include "iostream.h"

int main(){
	std::string mail = "admin-web.com";
	std::string at = "@";
	if (mail.find(at) == std::string::npos) {
		std::cout << "invalid";
	}
}


//!StrstrIfStatementSecondArg1
//@main.cpp
#include <cstring>
#include "iostream.h"

int main(){
	char at[] = "@";
	char mail[] = "admin-web.com";
	if(nullptr == strstr(mail, at)){
		std::cout << "invalid";
	}
}
//=
#include <cstring>
#include <string>
#include "iostream.h"

int main(){
	std::string at = "@";
	char mail[] = "admin-web.com";
	if (nullptr == strstr(mail, at.c_str())) {
		std::cout << "invalid";
	}
}


//!StrstrIfStatementSecondArg2
//@main.cpp
#include <cstring>
#include <string>
#include "iostream.h"

int main(){
	std::string mail = "admin-web.com";
	char at[] = "@";
	if (mail.find(at) == std::string::npos) {
		std::cout << "invalid";
	}
}
//=
#include <cstring>
#include <string>
#include "iostream.h"

int main(){
	std::string mail = "admin-web.com";
	std::string at = "@";
	if (mail.find(at) == std::string::npos) {
		std::cout << "invalid";
	}
}


//!StrstrAssignedToCharPointer1
//@main.cpp
#include <cstring>
int main() {
	char str[] = "a string";
	char* found = strstr(str, "string");
}
//=
#include <cstring>
#include <string>
int main() {
	std::string str = "a string";
	char* found = strstr(&*str.begin(), "string");
}


//!StrstrAssignedToCharPointer2
//@main.cpp
#include <cstring>
int main() {
	char str[] = "a string";
	char* found;
	found = strstr(str, "string");
}
//=
#include <cstring>
#include <string>
int main() {
	std::string str = "a string";
	char* found;
	found = strstr(&*str.begin(), "string");
}


//!StrstrAssignedToConstCharPointer1
//@main.cpp
#include <cstring>
int main() {
	char str[] = "a string";
	const char* found = strstr(str, "string");
}
//=
#include <cstring>
#include <string>
int main() {
	std::string str = "a string";
	const char* found = strstr(str.c_str(), "string");
}


//!StrstrAssignedToConstCharPointer2
//@main.cpp
#include <cstring>
int main() {
	char str[] = "a string";
	const char* found;
	found = strstr(str, "string");
}
//=
#include <cstring>
#include <string>
int main() {
	std::string str = "a string";
	const char* found;
	found = strstr(str.c_str(), "string");
}

